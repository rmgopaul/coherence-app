# Execution Log

Chronological log of phase/task progress against [execution-plan.md](./execution-plan.md). Append entries as work ships; do not rewrite history.

| Date | Phase / Task | Status | PR | Notes |
| ---- | ------------ | ------ | -- | ----- |
| 2026-04-23 | Task 1.1a — Health Connect triage | Done | [#8](https://github.com/rmgopaul/coherence-app/pull/8) | Findings at [docs/triage/health-connect-findings.md](./triage/health-connect-findings.md). Root cause: CORS blockage on `x-sync-key`. |
| 2026-04-24 | Task 1.1b — Health Connect fix | Done | [#10](https://github.com/rmgopaul/coherence-app/pull/10) | `x-sync-key` added to Samsung webhook CORS allowedHeaders. |
| 2026-04-24 | Task 1.1 follow-up | Done | [#14](https://github.com/rmgopaul/coherence-app/pull/14) | Annotated single-user design boundary at `server/oauth-routes.ts:440` + `server/_core/pinGate.ts:45`. |
| 2026-04-23 | Task 1.2a — Cloud-sync triage | Done | N/A | Findings at [docs/triage/cloud-sync-findings.md](./triage/cloud-sync-findings.md). Critical finding F4: cloud-manifest paths keyed by `userId`, blocking cross-user visibility. |
| 2026-04-24 | Task 1.2b — Schema migration (scopeId column + backfill) | Done | [#45](https://github.com/rmgopaul/coherence-app/pull/45) | Additive: added `scopeId` to `solarRecDashboardStorage` + `solarRecDatasetSyncState`; backfilled to `scope-user-${userId}`; added new indexes alongside legacy ones. |
| 2026-04-24 | Task 1.2b — Scope-keyed procedure rewrites + read-compat shim | Done | [#46](https://github.com/rmgopaul/coherence-app/pull/46) | 4 DB helpers + S3 path builder now filter/write by scopeId internally (zero call-site churn). `loadDashboardPayload` falls back to legacy per-user S3 paths on cache miss. |
| 2026-04-24 | Task 1.2b — S3 migration script | Done | [#47](https://github.com/rmgopaul/coherence-app/pull/47), [#48](https://github.com/rmgopaul/coherence-app/pull/48) | Ships `pnpm solarrec:migrate-scope` (copy-only, idempotent). #48 fixed missing `dotenv/config` import. **Decision: script is available but not run.** Dry-run showed 32,446 copy pairs; cost/benefit favors leaving the shim permanent — DB is authoritative (every row scope-backfilled in #45), S3 fallback rarely fires on the hot path. |
| 2026-04-24 | **Phase 1 gate** | **ACK** | — | Rhett's health data is flowing again (1.1 complete). Cloud sync model is truthful and cross-user (1.2b reads/writes resolve by scope; legacy user paths remain readable via the shim). Phase 2 may begin. |
| 2026-04-24 | Task 2.1 — DropDock Sheets title enrichment mismatch | Done | [#12](https://github.com/rmgopaul/coherence-app/pull/12) server-side accept both; [#50](https://github.com/rmgopaul/coherence-app/pull/50) client emits spreadsheetId | Client `UniversalDropDock.tsx` emitted `sheetId` while server read `spreadsheetId`. Server accepts both; client standardized. |
| 2026-04-24 | Task 2.2 — DropDock Calendar non-primary calendars | Done | [#13](https://github.com/rmgopaul/coherence-app/pull/13) server decodes calendarId from eid; [#51](https://github.com/rmgopaul/coherence-app/pull/51) client forwards calendarId | Server's decode-from-eid fallback only fired when `meta.eventId` was absent; client was sending both, so calendarId stayed undefined. Client now decodes base64 eid and forwards calendarId explicitly. |
| 2026-04-24 | Task 2.3 — ABP Settlement cross-month contamination | **Deferred → Done 2026-04-27** | — | Per plan note, deferred to Phase 5 with the ABP Settlement migration (Task 5.9 PR-A #133); the migration shipped first to keep that PR focused on mechanics. The actual fix shipped separately on 2026-04-27 — see the Task 2.3 row near the bottom of this log. |
| 2026-04-24 | Task 2.4 — Google token refresh race | Done | [#17](https://github.com/rmgopaul/coherence-app/pull/17) | Single-flight `Map<string, Promise<string>>` keyed by `${userId}:${provider}` in `tokenRefresh.ts`. |
| 2026-04-24 | Task 2.5 — `todayKey` lag across midnight | Done | [#16](https://github.com/rmgopaul/coherence-app/pull/16) | `useSyncExternalStore` subscribed to `setTimeout(msUntilMidnight)` ticks. |
| 2026-04-24 | Task 2.6 — DropDock canonicalization preserves auth-bearing params | Done | [#15](https://github.com/rmgopaul/coherence-app/pull/15) server strip + `hasSensitiveParams` helper; [#52](https://github.com/rmgopaul/coherence-app/pull/52) UI warn in UniversalDropDock | `SENSITIVE_PARAMS` set added to canonicalization; DropDock.tsx already surfaced the toast; UniversalDropDock was the last entry path missing it. |
| 2026-04-24 | Task 2.7 — Samsung webhook per-device HMAC | Closed (no-op) | [#14](https://github.com/rmgopaul/coherence-app/pull/14) | Plan decision: main is single-user by design; shared sync key is permanent. Annotation comments added at the webhook + pin-gate sites. |
| 2026-04-24 | **Phase 2 gate** | **ACK** | — | Every known user-facing bug is fixed (Task 2.3 deliberately deferred to Phase 5). Phase 3 was shipped in parallel (see below). Phase 4 may continue. |
| 2026-04-24 | Task 3.1 — Scheduler refactor with distributed claim | Done | [#21](https://github.com/rmgopaul/coherence-app/pull/21) | `dailyJobClaims(dateKey, runKey)` table + `scheduleDaily` helper. Restart + two-instance safe. |
| 2026-04-24 | Task 3.2 — `monitoringApiRuns` retention prune | Done | [#20](https://github.com/rmgopaul/coherence-app/pull/20) | Nightly prune of rows > 365 days. |
| 2026-04-24 | Task 3.3 — Gmail waiting-on server-side cache | Done | [#22](https://github.com/rmgopaul/coherence-app/pull/22) | 15-min TTL cache via `gmailWaitingOnCache` table. |
| 2026-04-24 | Task 3.4 — WHOOP refetch cadence | Done | [#18](https://github.com/rmgopaul/coherence-app/pull/18) | `staleTime: 20m`, `refetchInterval: 30m`. |
| 2026-04-24 | Task 3.5 — DropDock enrichment race | Done | [#19](https://github.com/rmgopaul/coherence-app/pull/19) | Input disabled while in-flight; 200ms paste cooldown via `pasteCooldownRef`. |
| 2026-04-24 | **Phase 3 gate** | **ACK** | — | Scheduler, retention, caching, cadence, and race guards all in place. Phase 4 + Phase 5 unblocked. |
| 2026-04-24 | Task 4.1 — Establish solar-rec as business-functions home | Done | [#24](https://github.com/rmgopaul/coherence-app/pull/24) | CLAUDE.md dual-router boundary doc, `docs/architectural-split.md`, lint rule for cross-imports. |
| 2026-04-24 | Task 4.2 — Remove Enphase V2 code | Done | [#26](https://github.com/rmgopaul/coherence-app/pull/26) | V2 deprecated; active code removed. Historical enum values kept for monitoringApiRuns integrity. |
| 2026-04-24 | Task 4.3 — Command palette global search wiring | Done | [#28](https://github.com/rmgopaul/coherence-app/pull/28) (debounced split) + [#54](https://github.com/rmgopaul/coherence-app/pull/54) (Task 4.3b: per-result secondary actions) | Debounced 200ms `search.global`; split Commands / Results groups with per-type navigation. PR #54 added per-result secondary actions: dock (`trpc.dock.add`), create note (`trpc.notes.create`), pin as King of the Day (`trpc.kingOfDay.pin`). Buttons appear inline on each search result (hover/focus reveal); event-bubble guards prevent the secondary-action click from firing the row-level `onSelect`. Each handler calls the appropriate mutation, shows a `sonner` toast on success/failure, and (for "create note") closes the palette + navigates to the new note. |
| 2026-04-24 | Task 4.4 — Consolidate date-key helpers | Done | [#23](https://github.com/rmgopaul/coherence-app/pull/23) + [#25](https://github.com/rmgopaul/coherence-app/pull/25) | `shared/dateKey.ts` introduced; 26 call sites migrated. |
| 2026-04-24 | Task 4.5 — Shared "Ask AI about this data" component | Done (incremental rollout completed by 2026-04-27) | [#31](https://github.com/rmgopaul/coherence-app/pull/31) initial component + Notebook + per-module model prefs + conversation persistence; [#67](https://github.com/rmgopaul/coherence-app/pull/67), [#68](https://github.com/rmgopaul/coherence-app/pull/68), [#69](https://github.com/rmgopaul/coherence-app/pull/69), [#70](https://github.com/rmgopaul/coherence-app/pull/70) Solar REC standalone pages; subsequent rollout absorbed into the Phase 5 page-migration + tab-extraction PRs (#107–#146). | All four sub-deliverables done: (1) shared `AskAiPanel` at `client/src/components/AskAiPanel.tsx`; (2) per-module model preference via `trpc.anthropic.{getModelForModule,setModelForModule}` (loaded on mount, written-through on dropdown change, scoped by `moduleKey`); (3) conversation persistence to `conversations` / `messages` tables with `source: ask-ai:${moduleKey}` (best-effort — failures don't block the answer per the comment at `personalData.ts:2702`); (4) deployed to all 31 target pages: 20 Solar REC dashboard tabs + 6 standalone Solar REC pages (AbpInvoiceSettlement, EarlyPayment, InvoiceMatchDashboard, ContractScanner, DinScrapeManager, ZendeskTicketMetrics) + 5 main-app pages (Notebook, FrontPageDashboard, SupplementsInsightsPanel, HabitsInsightsPanel, HealthInsightsPanel). Each deployment passes its own `contextGetter`. The original "rollout: 1-2 deployments per PR" pace was largely respected — most of the standalone-page deployments shipped alongside their respective Phase 5 Task 5.x migration PRs. |
| 2026-04-24 | Task 4.6 — Todoist task from Notebook text selection | Done | [#27](https://github.com/rmgopaul/coherence-app/pull/27) + [#29](https://github.com/rmgopaul/coherence-app/pull/29) + [#30](https://github.com/rmgopaul/coherence-app/pull/30) | Cmd+Alt+T (not Cmd+T — Chrome captures); plan + CLAUDE.md updated to reflect the browser-shortcut pitfall. |
| 2026-04-24 | Task 4.7 — MeterReadsPage migration | Done | [#32](https://github.com/rmgopaul/coherence-app/pull/32)–[#43](https://github.com/rmgopaul/coherence-app/pull/43), [#39](https://github.com/rmgopaul/coherence-app/pull/39) (Tesla Solar removal), [#44](https://github.com/rmgopaul/coherence-app/pull/44) (Tesla Powerhub cleanup) | All 9 vendors on shared `MeterReadsPage`; shared config grew `bulkDataTypes`, `noBulkFetch`, `select` credential fields, `preConnectContent`, `accessKeyIdMasked`, `usernameMasked`. Tesla Solar removed rather than migrated (broken integration). ~−10K LOC net. |
| 2026-04-24 | **Phase 4 gate** | **ACK** | — | Architecture formalized, Enphase V2 gone, meter-reads are one page, AskAiPanel is deployable. Task 4.3b and 4.5 V2+ deployments are explicitly deferred follow-ups — not gate blockers. Phase 5 prerequisites (Task 1.2b) already acked. Phase 5 may begin. |
| 2026-04-24 | Task 4.5 V3 — AskAiPanel rollout to 8 solar-rec pages | Done | [#67](https://github.com/rmgopaul/coherence-app/pull/67) Data Quality + Delivery Tracker, [#68](https://github.com/rmgopaul/coherence-app/pull/68) ABP Settlement + Early Payment, [#69](https://github.com/rmgopaul/coherence-app/pull/69) Invoice Match + Contract Scanner, [#70](https://github.com/rmgopaul/coherence-app/pull/70) DIN Scrape Manager + Zendesk Metrics | Eight pages now embed the shared `AskAiPanel`. Per-page model preference + conversation persistence still deferred to a later V — the panel reuses the shared `anthropic.ask` endpoint introduced in #31. |
| 2026-04-24 | Task 5.1 PR 1 — permission-matrix schema + module registry | Done | [#71](https://github.com/rmgopaul/coherence-app/pull/71) | New tables `solarRecUserModulePermissions` (unique on `userId, scopeId, moduleKey`) and `solarRecPermissionPresets`. `ownerUserId` added to `solarRecScopes` (backfilled to Rhett); `isScopeAdmin BOOLEAN` added to `solarRecUsers`. Canonical module keys in `shared/solarRecModules.ts`. |
| 2026-04-24 | Task 5.1 PR 2 — `requirePermission` middleware + permissions router + hook | Done | [#72](https://github.com/rmgopaul/coherence-app/pull/72) | tRPC procedure builder `.requirePermission(moduleKey, minLevel)` enforces `read`/`edit`/`admin` ladder server-side; `useSolarRecPermission(moduleKey)` hook returns `{canRead, canEdit, canAdmin, isScopeAdmin}` for client-side gating. Scope owner + `isScopeAdmin=true` bypass the matrix to prevent lockout. |
| 2026-04-24 | Task 5.1 PR 3 — Team & Permissions matrix editor in Settings | Done | [#73](https://github.com/rmgopaul/coherence-app/pull/73) | New "Team & Permissions" tab in Solar REC Settings. Grid editor: users on rows, modules on columns, dropdown per cell. Visible only to scope owner / scope admins / users with `admin` on `team-permissions`. |
| 2026-04-24 | Task 5.1 PR 4 — Permission preset manager | Done | [#74](https://github.com/rmgopaul/coherence-app/pull/74) | Admin-buildable preset library backed by `solarRecPermissionPresets`. Presets are reusable templates (snapshot semantics on apply — editing a preset later does not retroactively update users assigned it). |
| 2026-04-24 | Task 5.1 PR 5 — Retrofit standalone pages with permission gates | Done | [#75](https://github.com/rmgopaul/coherence-app/pull/75) | Existing `/solar-rec/*` pages (Monitoring, Settings, Team) now wrap routes in `<PermissionGate moduleKey="...">`. Pre-Phase-5 users (Rhett) default to admin on every module via the scope-owner bypass. |
| 2026-04-24 | Task 5.2 PR A — Bind invites to permission presets | Done | [#76](https://github.com/rmgopaul/coherence-app/pull/76) | "Invite teammate" form gains an optional preset selector. Invite stores the preset reference alongside the one-use token. |
| 2026-04-24 | Task 5.2 PR B — Apply preset on invite accept | Done | [#77](https://github.com/rmgopaul/coherence-app/pull/77) | Accept flow snapshots the chosen preset's permissions into `solarRecUserModulePermissions` for the new user. Empty preset = all `none`, admin sets manually. |
| 2026-04-24 | Auth fix — exempt OAuth callback from PIN gate | Done | [#78](https://github.com/rmgopaul/coherence-app/pull/78) | The `/api/oauth/callback` route was being intercepted by the PIN gate after Task 5.1 wiring; exempted explicitly so Google/Microsoft/Todoist callbacks aren't blocked. |
| 2026-04-24 | Task 5.1 follow-up — harden permission ACL | Done | [#79](https://github.com/rmgopaul/coherence-app/pull/79) | Owner bypass implemented in middleware (not in DB query); permission writes wrapped in transactions; pages re-verified gated; `useSolarRecPermission` cache TTL set to 60s. Pre-empts the 2026-04-24 auth outage that was caused by the `solarRecUsers.isScopeAdmin` migration not landing on prod before the schema deploy (see CLAUDE.md "Schema migration safety"). |
| 2026-04-24 | Task 5.3 — Migrate monitoring scheduler + `monitoringApiRuns` (scoping) | Done | [#80](https://github.com/rmgopaul/coherence-app/pull/80) | `scopeId` added to `monitoringApiRuns` + `monitoringBatchRuns`; reads/writes filter by scope; scheduler attribution attaches scope context. Module key: `monitoring-overview`. |
| 2026-04-24 → 2026-04-26 | Task 5.4 — Migrate meter-read pages to team credentials | Done | [#81](https://github.com/rmgopaul/coherence-app/pull/81) Generac, [#82](https://github.com/rmgopaul/coherence-app/pull/82) Solis, [#83](https://github.com/rmgopaul/coherence-app/pull/83) GoodWe, [#84](https://github.com/rmgopaul/coherence-app/pull/84) Hoymiles, [#86](https://github.com/rmgopaul/coherence-app/pull/86) Locus, [#88](https://github.com/rmgopaul/coherence-app/pull/88) APsystems, [#89](https://github.com/rmgopaul/coherence-app/pull/89) SolarLog, [#90](https://github.com/rmgopaul/coherence-app/pull/90) Growatt, [#91](https://github.com/rmgopaul/coherence-app/pull/91) EKM, [#92](https://github.com/rmgopaul/coherence-app/pull/92) Fronius, [#93](https://github.com/rmgopaul/coherence-app/pull/93) EnnexOS, [#95](https://github.com/rmgopaul/coherence-app/pull/95) Enphase V4, [#96](https://github.com/rmgopaul/coherence-app/pull/96) SolarEdge, [#99](https://github.com/rmgopaul/coherence-app/pull/99) Tesla Powerhub (re-delivery of [#98](https://github.com/rmgopaul/coherence-app/pull/98) which merged into the deleted SolarEdge branch), [#100](https://github.com/rmgopaul/coherence-app/pull/100) SunPower, [#102](https://github.com/rmgopaul/coherence-app/pull/102) eGauge | All 16 vendor pages on `client/src/solar-rec/pages/meter-reads/`, gated on `meter-reads:read`/`:edit`, backed by `solarRecTeamCredentials[provider=…]`. Five vendors took special-case shapes: Enphase V4 (OAuth refresh persisted to credential row), SolarEdge (3 snapshot types), Tesla Powerhub (single bulk-group endpoint with 5-min cache), SunPower (no upstream API — only the dashboard read path migrated; Expo `solarReadings.submit` stays on main), eGauge (each credential = one meter profile, 4 access types). The other 11 use a uniform `getStatus`/`listSites`/`getProductionSnapshot` pattern. Tesla Solar deferred — broken integration removed in #39, not migrated. PR #96 force-pushed once to remove user-WIP contamination (`monitoring.getGridPage` etc. referencing non-existent DB helpers). |
| 2026-04-24 | Health — wire Samsung Health snapshot in `HealthTodayPanel` | Done | [#85](https://github.com/rmgopaul/coherence-app/pull/85) | Today panel was reading the raw `summary` blob; now reads the canonical `samsung*` columns from `dailyHealthMetrics` so it tracks the same source as the historical view. |
| 2026-04-24 | Android — declare `foregroundServiceType=health` on WorkManager service | Done | [#87](https://github.com/rmgopaul/coherence-app/pull/87) | Without this declaration on Android 14+ the OS terminates the long-running sync worker. |
| 2026-04-25 | Samsung Health — wire `dailyHealthMetrics` on every sync; correct sleep total | Done | [#94](https://github.com/rmgopaul/coherence-app/pull/94) | Webhook ingest only wrote `samsungSyncPayloads` + `integrations.metadata`; Android `metricsRepo.getHistory` reads `dailyHealthMetrics`, populated only by the nightly snapshot. New `upsertSamsungDailyMetric` (server/db/metrics.ts) does a targeted samsung-only upsert with preserve-on-null fallback. Also flips `HealthConnectPayloadMapper.computeSleepMetrics` to prefer the SleepSessionRecord total over the merged stages sum. |
| 2026-04-25 | Android — truthful historical backfill (selectable 7/14, no ghost rows, partial state) | Done | [#97](https://github.com/rmgopaul/coherence-app/pull/97) | `SettingsScreen.kt:294` `coerceAtLeast(2)` snapped 7/14 to 30; removed. `HistoricalSyncWorker` no longer POSTs days where the rate limiter blocked the read (was creating zero-filled `dailyHealthMetrics` rows). Worker now tracks `partialSuccess` and surfaces "Backfill partial — some days were rate-limited" in the UI instead of green-checking incomplete runs. |
| 2026-04-25 | Samsung Health — scope `manualScores` to today; backfill clears stale historical scores | Done | [#101](https://github.com/rmgopaul/coherence-app/pull/101) | `manualScores` (the user's dashboard entry for today's sleep/energy) was being applied to every historical-backfill payload, stamping today's 75/93 across 30 days of history. Fix: only pass `manualScores` to `buildSamsungMetadata` when the payload date matches the live summary date. Companion fix: `upsertSamsungDailyMetric` no longer preserves nulls on `samsungSleepScore`/`samsungEnergyScore` (record-derived fields like steps still preserve, but score nulls are ground truth meaning "no manual entry today"). |
| 2026-04-26 | **Task 5.4 gate** | **ACK** | — | All 16 vendor meter-read pages migrated; `MeterReadsPage` legacy is now unreferenced from `SolarRecApp.tsx`. Phase 5 next moves to Task 5.5 (Solar REC Dashboard migration). |
| 2026-04-26 | Task 5.5 — Migrate Solar REC Dashboard | Done | TBD | `solarRecDashboardRouter` (3,558 LOC, 42 procedures) moved from `server/routers/solarRecDashboard.ts` to `server/_core/solarRecDashboardRouter.ts`. Procedures swap `protectedProcedure` → `requirePermission("solar-rec-dashboard", level)` and `ctx.user.id` → `ctx.userId`. Permission classification: 24 read (queries + debug) / 13 edit (mutations writing data) / 5 admin (destructive ops + force-runs + infra migrations). New `server/_core/solarRecBase.ts` extracts the shared `t` instance + `requirePermission` middleware so multiple sub-router source files can share without circular deps. `solarRecDashboard` re-added to `SOLAR_REC_ROUTER_ROOTS`; legacy `/solar-rec-dashboard` URL on `App.tsx` retired with a `<Redirect to="/solar-rec/dashboard" />`. 11 client files migrated: 8 single-root callers swap import as `import { solarRecTrpc as trpc } from "@/solar-rec/solarRecTrpc"`; 3 mixed-root files (AbpInvoiceSettlement, EarlyPayment, the dashboard page itself) add a second `solarRecTrpc` import alongside the existing `trpc`. `solarRecTrpc.Provider` now wired into both `client/src/main.tsx` (newly added) and `client/src/solar-rec-main.tsx` (already present); the splitLink hack in main.tsx that routed `solarRecDashboard.*` to a special URL is gone. CLAUDE.md and `docs/server-routing.md` updated. tsc clean; 540/540 vitest pass. |
| 2026-04-26 | **Task 5.5 gate** | **ACK** | — | Solar REC Dashboard now lives on the standalone Solar REC router. Phase 5 next moves to Task 5.6 (Schedule B import + CSG Schedule B import migration) — note Schedule B procedures already migrated as part of Task 5.5 since they're inside `solarRecDashboardRouter`. Task 5.6 may be largely complete; verify before starting. |
| 2026-04-26 | Task 5.6 PR-A — Schedule B procedures use `schedule-b` module key | Done | [#113](https://github.com/rmgopaul/coherence-app/pull/113) | The 13 Schedule B procedures inside `solarRecDashboardRouter` (`ensureScheduleBImportJob`, `linkScheduleBDriveFolder`, `importScheduleBFromCsgPortal`, `getScheduleBImportStatus`, `listScheduleBImportResults`, `applyScheduleBToDeliveryObligations`, `getScheduleBContractIdMapping`, `applyScheduleBContractIdMapping`, `uploadScheduleBFileChunk`, `forceRunScheduleBImport`, `clearScheduleBImport`, `clearScheduleBImportStuckUploads`, `debugScheduleBImportRaw`) reclassified from `requirePermission("solar-rec-dashboard", level)` to `requirePermission("schedule-b", level)`. Permission level distribution unchanged (4 read / 6 edit / 3 admin). No schema change; no client change required. tsc clean; 540/540 vitest pass. |
| 2026-04-26 | Task 5.6 PR-B — Schedule B tables get `scopeId` tenancy key | Done | [#115](https://github.com/rmgopaul/coherence-app/pull/115) | Migration `0036_schedule_b_scope_id.sql` adds `scopeId varchar(64) NOT NULL` + scope index to all 4 `scheduleBImport*` tables (jobs, files, results, csgIds). Backfill: jobs.scopeId set to `CONCAT('scope-user-', userId)`; the 3 child tables join to jobs on `jobId` and copy. ADD COLUMN with empty-string default → backfill UPDATE → ALTER DROP DEFAULT pattern keeps the migration safe on a populated table. DB helpers updated: `createScheduleBImportJob({userId, scopeId})`, `getLatestScheduleBImportJob(scopeId)`, `getOrCreateLatestScheduleBImportJob(scopeId, userId)`, plus a private `resolveScopeIdForJob(jobId)` reads the parent job's scope so the 4 child-table insert helpers (`upsertScheduleBImportFileUploadProgress`, `markScheduleBImportFileQueued`, `bulkInsertScheduleBDriveFiles`, `upsertScheduleBImportResult`, `bulkInsertScheduleBImportCsgIds`) all populate `scopeId` without forcing every caller to pass it. 13 call sites in `solarRecDashboardRouter` mass-replaced from `ctx.userId` to `ctx.scopeId` (and `ctx.scopeId, ctx.userId` for the get-or-create helper). tsc clean; 540/540 vitest pass. Prod migration applied + 15 orphan child rows backfilled to `scope-user-1` before merge. |
| 2026-04-26 | Task 5.7 PR-A — Contract scan tables get `scopeId` tenancy key | Done | [#117](https://github.com/rmgopaul/coherence-app/pull/117) | Migration `0037_contract_scan_scope_id.sql` mirrors PR-B's pattern for the 3 `contractScan*` tables (jobs / jobCsgIds / results). DB helpers in `server/db/contractScans.ts`: `createContractScanJob({userId, scopeId, totalContracts})`, `getLatestContractScanJob(scopeId)`, `listContractScanJobs(scopeId, limit)`, `getLatestScanResultsByCsgIds(scopeId, csgIds)`, `updateContractScanResultOverrides(scopeId, csgId, overrides)`. The latter two **drop the JOIN** they previously needed for cross-tenant isolation — `contractScanResults.scopeId` is denormalized so a direct `WHERE scopeId = ?` is equivalent and one fewer table scanned. New private `resolveScopeIdForJob(jobId)` reads the parent job's scope for child-table inserts. `contractScanJobRunner.ts` passes `job.scopeId` on its `insertContractScanResult` call. 5 call sites in `jobRunners.ts` updated to compute scope inline via `resolveSolarRecScopeId()` (single-tenant prod returns `scope-user-${ownerUserId}` regardless of caller). tsc clean; 540/540 vitest pass. Prod migration applied + verified clean (zero orphans this time). |
| 2026-04-26 | Task 5.7 PR-B — Move contract-scan procs + pages to standalone | Done | [#118](https://github.com/rmgopaul/coherence-app/pull/118) | New `server/_core/solarRecContractScanRouter.ts` houses the 11 db/result/override procs that previously lived in main `abpSettlementRouter` (`startDbContractScanJob`, `stopDbContractScanJob`, `deleteDbContractScanJob`, `resumeDbContractScanJob`, `getDbJobStatus`, `listDbContractScanJobs`, `getDbContractScanResults`, `exportDbContractScanResultsCsv`, `getContractScanResultsByCsgIds`, `updateContractOverride`, `rescanSingleContract`). All gated on `requirePermission("contract-scrape-manager", level)` — 7 read / 3 edit / 1 admin (delete). Cross-tenant safety: ownership checks switched from `job.userId !== ctx.user.id` to `job.scopeId !== ctx.scopeId` per the architectural-split rule "data visibility is team-wide within a scope". `solarRecAppRouter` composition + `SOLAR_REC_ROUTER_ROOTS` gain `contractScan`. Client: `ContractScrapeManager.tsx` and `ContractScanner.tsx` moved from `client/src/features/dashboard/` to `client/src/solar-rec/pages/`. `ContractScrapeManager` aliases `solarRecTrpc as trpc` and mass-replaces `trpc.abpSettlement.*` → `trpc.contractScan.*`. `ContractScanner` is a pure client-side PDF parser (no trpc) wrapped in `<PermissionGate moduleKey="contract-scanner">`. `FinancialsTab` (inside `solar-rec-dashboard/components/`) and the `getContractScanResultsByCsgIds` call in `SolarRecDashboard.tsx` switched to the standalone client. App.tsx replaces `/contract-scanner` + `/contract-scrape-manager` Routes with Wouter `<Redirect />` to the new `/solar-rec/contract-scanner` + `/solar-rec/contract-scrape-manager` URLs. tsc clean; 540/540 vitest pass. |
| 2026-04-27 | Task 5.8 PR-A — DIN scrape tables get `scopeId` tenancy key | Done | TBD | Migration `0038_din_scrape_scope_id.sql` mirrors PR #117/#115's pattern for the 4 `dinScrape*` tables (jobs / jobCsgIds / results / dins). DB helpers in `server/db/dinScrapes.ts`: `createDinScrapeJob({userId, scopeId, totalSites})`, `listDinScrapeJobs(scopeId, limit)`, plus a private `resolveScopeIdForJob(jobId)` reads parent job's scope for child-table inserts (`bulkInsertDinScrapeJobCsgIds`, `persistDinScrapeSiteResult` — the latter sets scopeId on both the result row and every dins row in the same transaction). 1 call site in `jobRunners.ts dinScrapeRouter` updated for `createDinScrapeJob`, 1 for `listDinScrapeJobs`. Runner (`server/services/core/dinScrapeJobRunner.ts`) passes `job.scopeId` on `persistDinScrapeSiteResult`. Same drizzle-kit collision pattern as PR-B/PR-A — renamed to 0038, restored 0034 snapshot, fixed journal idx 34 tag. tsc clean; 540/540 vitest pass. Prod migration applied + verified clean (zero orphans across all 4 tables). **Follow-up PR-B** still needs to move the procs from main `dinScrapeRouter` to the standalone Solar REC router with `requirePermission("din-scrape-manager", level)` and move the `DinScrapeManager.tsx` page. |
| 2026-04-27 | Task 5.11 PR-A — Migrate Zendesk Metrics to standalone | Done | TBD | New `server/_core/solarRecZendeskRouter.ts` houses the 5 procs that previously lived in main `zendeskRouter` (`getStatus`, `connect`, `saveTrackedUsers`, `disconnect`, `getTicketMetrics`). All gated on `requirePermission("zendesk-metrics", level)` — 2 read / 1 edit / 2 admin. `solarRecAppRouter` composition + `SOLAR_REC_ROUTER_ROOTS` gain `zendesk`. Client: `ZendeskTicketMetrics.tsx` moved from `client/src/features/dashboard/` to `client/src/solar-rec/pages/`; aliases `solarRecTrpc as trpc` so every `trpc.zendesk.*` call site stays unchanged. `server/routers/solarMisc.ts` deleted — that file's only export was `zendeskRouter` after the 2026-04-26 cleanup (#109). App.tsx replaces `/zendesk-ticket-metrics` Route with Wouter `<Redirect />` to the new `/solar-rec/zendesk-ticket-metrics` URL. No schema migration. **Task 5.11 partial**: AddressChecker (PR-B) blocked on Task 5.9 (its `trpc.abpSettlement.cleanMailingData` + `verifyAddresses` calls migrate with ABP Settlement); DeepUpdateSynthesizer (PR-C) blocked on the dashboard data-flow refactor (its `solarRecDashboard.getDataset` / `saveDataset` calls are being reshaped by that plan's PR-2). tsc clean; 540/540 vitest pass. |
| 2026-04-26 → 2026-04-27 | Data-flow OOM diagnosis arc | Done | [#107](https://github.com/rmgopaul/coherence-app/pull/107) added direct row-table hydration as a perf optimization, [#111](https://github.com/rmgopaul/coherence-app/pull/111) excluded the 2 largest tables from the bulk hydration when server-side memory blew, [#114](https://github.com/rmgopaul/coherence-app/pull/114) fully disabled the row-table hydration mutation after the same payload OOM'd Chrome tabs on JSON.parse | Cause was the same on both ends: a single tRPC response materializing a full `CsvRow[]` for `transferHistory`/`accountSolarGeneration` (50–150 MB JSON peaks ~2-3× during parse). `_runnerVersion` markers proved the deploys landed before the wholesale architectural review opened. The `getDatasetRowsFromSrDs` mutation was left in place as a disabled stub until PR-8 below removed it. |
| 2026-04-27 | Solar REC dashboard data-flow PR-1 — diagnostic surface | Done | [#120](https://github.com/rmgopaul/coherence-app/pull/120) | New `debugDatasetPersistenceRaw(datasetKey)` query reads raw rows from every persistence layer (`solarRecDashboardStorage`, `solarRecDatasetSyncState`, `solarRecActiveDatasetVersions`, `srDs*` row count) and returns a verdict classifier (`consistent` / `storage-only` / `db-only` / `row-table-stale` / `no-active-batch` / `missing`). Same response carries `_runnerVersion: "data-flow-pr1"` so deploys are verifiable from devtools. This is the diagnostic prerequisite that all later PRs in the series reference: before chasing a "Cloud sync failed" hypothesis, read the verdict. |
| 2026-04-27 | Solar REC dashboard data-flow PR-2 — make `dbPersisted=true` required for "Cloud Verified" | Done | [#121](https://github.com/rmgopaul/coherence-app/pull/121) | Root cause of the LOCAL-ONLY-NEVER-PERSISTS bug: `isChildKeyRecoverable` in `server/services/solar/datasetCloudStatus.ts` accepted "S3 blob exists" as recoverable evidence, so a partial write that flushed to storage without a DB row showed "Cloud Verified" indefinitely. Fix: `isChildKeyRecoverable` now requires `record.dbPersisted === true` AND `payloadBytes > 0`. Top-level dataset is gated on `syncRow.dbPersisted === true`. `saveDataset` surfaces `dbError: string | null` in the response (replacing silent `console.warn`); the client UI renders the actual error. New `datasetCloudStatus.test.ts` with 4 cases asserting the new contract. `_runnerVersion: "data-flow-pr2"`. tsc clean; 540/540 vitest pass. |
| 2026-04-27 | Solar REC dashboard data-flow PR-3 — `getDatasetSummariesAll` | Done | [#123](https://github.com/rmgopaul/coherence-app/pull/123) | New tRPC query returns `{rowCount, byteCount, cloudStatus, lastUpdated, isRowBacked}` for all 18 datasets in a single ~5 KB response. `rowCount` for the 7 row-backed datasets reads `srDs*` directly via the active batch; for the 11 non-row-backed datasets it reads the chunked-CSV manifest's `rowCount` if persisted. Eliminates the need for the client to load full row arrays just to display counts. `_runnerVersion: "data-flow-pr3"`. PR #122 was a phantom merge (the `gh pr edit --base` shuffle during stacking); the actual content survived as #123. |
| 2026-04-27 | Solar REC dashboard data-flow PR-4 — cursor-paginated row reader | Done | [#124](https://github.com/rmgopaul/coherence-app/pull/124) | New `getDatasetRowsPage(datasetKey, cursor, limit)` query for the 7 row-backed datasets. Keyset pagination via `gt(table.id, cursor)` — no offset cost. Page sizes capped at the wire-payload limit (~30–300 KB per page). `buildSystemSnapshot.ts` gains exported `loadDatasetRowsPage(scopeId, batchId, table, {cursor, limit})` and `reconstructCsvRow(row, remap)` helpers; the existing `loadDatasetRows` was refactored to call `reconstructCsvRow` rather than reproduce the typed-column-to-CSV-key remap inline. `_runnerVersion: "data-flow-pr4"`. tsc clean; 540/540 vitest pass. |
| 2026-04-27 | Solar REC dashboard data-flow PR-5 — server-side CSV export | Done | [#125](https://github.com/rmgopaul/coherence-app/pull/125) | New `getDatasetCsv(datasetKey)` query builds a CSV from `loadDatasetRowsPage` reads server-side and returns it as a single string (≤25 MB cap). Replaces the legacy client-side reassembly path that required loading every chunked-CSV blob and concatenating in the browser. The 7 row-backed datasets now go through the row tables; the 11 non-row-backed continue to call `getDatasetAssembled` until Task 5.12 completes. `_runnerVersion: "data-flow-pr5"`. |
| 2026-04-27 | Solar REC dashboard data-flow PR-6 — Total-Rows readout reads server summaries | Done | [#126](https://github.com/rmgopaul/coherence-app/pull/126) | `SolarRecDashboard.tsx` adds the `getDatasetSummariesAll` query (`staleTime: 30s`) and memoizes the result into `datasetSummariesByKey`. `dataHealthSummary.totalRowsLoaded` (the sticky-header strip displayed on every tab) prefers the server-side `summary.rowCount` over `datasets[key].rows.length`. Falls back to the in-memory length only when no summary exists yet. The header strip is now accurate even before (or after) the row arrays have been materialized into JS heap. `_runnerVersion: "data-flow-pr6"` on the underlying query. |
| 2026-04-27 | Solar REC dashboard data-flow PR-7 — Data Quality tab reads server summaries | Done | [#128](https://github.com/rmgopaul/coherence-app/pull/128) | `DataQualityTab.tsx` gains an optional `datasetSummariesByKey` prop. The freshness table (10 datasets) prefers `summary.lastUpdated` and `summary.rowCount` over `ds.uploadedAt` / `ds.rows.length`. The component degrades gracefully if the parent doesn't pass the prop yet. The cross-reference reconciliation between `deliveryScheduleBase` and `convertedReads` continues to read in-memory rows because `convertedReads` is not yet row-backed; that migration is captured in Task 5.12. `_runnerVersion` propagated via the underlying query. |
| 2026-04-27 | Solar REC dashboard data-flow PR-8 — lock the architecture in CLAUDE.md + dead code sweep | Done | [#130](https://github.com/rmgopaul/coherence-app/pull/130) | Adds the canonical "Solar REC Dashboard data flow (canonical, post 2026-04-27)" section to CLAUDE.md: source-of-truth-per-dataset table, wire-payload-contract table with max sizes, list of the 6 tabs that still need migration off raw rows (Task 5.13), persistence write contract, diagnostic surface description, and 4 hard rules (no >5,000-row materialization in tRPC; no `datasets[key].rows.length` reads on core dataset keys; `_runnerVersion` required on every new procedure; no silent error swallowing in persistence paths). Also removes the dead `getDatasetRowsFromSrDs` stub from `solarRecDashboardRouter.ts` plus the corresponding mutation hook + ref in `SolarRecDashboard.tsx` (the disabled-pending-rewrite path from #114). |
| 2026-04-27 | **Data-flow series gate** | **ACK** | — | The 7 row-backed datasets are end-to-end on the new pattern: `srDs*` canonical, `dbPersisted=true` required for the badge, server-side aggregation/pagination/export, sticky-header + Data Quality tab consume server summaries instead of in-memory rows. Wholesale architectural review request closed. Two follow-up workstreams written up as Task 5.12 (migrate the remaining 11 datasets to `srDs*`) and Task 5.13 (migrate the 6 deferred tabs off raw rows). Task 5.11 PR-C (Deep Update Synthesizer) was unblocked by PR-2's backward-compatible `dbError` addition and shipped immediately afterward as #131 (next row). |
| 2026-04-27 | Task 5.11 PR-C — Migrate Deep Update Synthesizer to standalone | Done | [#131](https://github.com/rmgopaul/coherence-app/pull/131) | Pure file move + permission gate. `DeepUpdateSynthesizer.tsx` (1098 LOC) moved from `client/src/features/dashboard/` to `client/src/solar-rec/pages/`. The page already imports `solarRecTrpc as trpc` (set up in Task 5.5 PR #110) so no proc swap is needed — its `solarRecDashboard.getDataset` / `saveDataset` calls already route through the standalone tRPC client. App.tsx legacy `/deep-update-synthesizer` URL replaced with Wouter `<Redirect />` to `/solar-rec/deep-update-synthesizer`; SolarRecApp.tsx adds the new route wrapped in `<PermissionGate moduleKey="deep-update-synthesizer">`. The data-flow PR-2 (#121) saveDataset signature change (`dbError: string | null` added) is backward-compatible at the call site — existing UI continues to work and now surfaces actual error messages when DB writes fail. No server-side changes; no schema migration. tsc clean; 544/544 vitest pass. **Task 5.11 still partial**: AddressChecker (PR-B) remains blocked on Task 5.9. |
| 2026-04-27 | Task 5.9 PR-A + Task 5.11 PR-B — Migrate ABP Invoice Settlement + Address Checker | Done | [#133](https://github.com/rmgopaul/coherence-app/pull/133) | Two new standalone routers: `server/_core/solarRecAbpSettlementRouter.ts` (7 procs gated on `abp-invoice-settlement` — `startContractScanJob` edit, `getJobStatus` read, `cleanMailingData` edit, `verifyAddresses` edit, `saveRun` edit, `getRun` read, `listRuns` read) and `server/_core/solarRecCsgPortalRouter.ts` (3 procs gated on `solar-rec-settings` — `status` read, `saveCredentials` admin, `testConnection` admin; CSG portal credentials are settings, not settlement state). Storage layer for the run-payload procs (`saveRun`/`getRun`/`listRuns`) keeps the existing `readPayloadWithFallback`/`writePayloadWithFallback` helpers but resolves `ownerUserId = await resolveSolarRecOwnerUserId()` and passes that, so every team member sees the same run history in single-tenant prod (full storage rewrite to scope-keyed S3 keys is deferred). Cross-scope safety on `getJobStatus` switched from `userId` to comparing `job.userId` against the resolved owner. CSG portal procs continue to use `ctx.userId` for the per-user `integrations` table (multi-user team-shared CSG access is future work — single-tenant prod has only Rhett's tokens). `csgPortalRouter` + `abpSettlementRouter` removed from `server/routers/jobRunners.ts` (file shrinks from ~790 LOC to 318) and from `server/routers.ts` composition; only `dinScrapeRouter` remains there pending Task 5.8 PR-B. `solarRecAppRouter` composition + `SOLAR_REC_ROUTER_ROOTS` gain `abpSettlement` + `csgPortal`. Client: `AbpInvoiceSettlement.tsx` (4117 LOC) moved from `client/src/features/dashboard/` to `client/src/solar-rec/pages/`; the dual-import (separate `trpc` for abp/csg + `solarRecTrpc` for dashboard) collapsed into a single aliased `solarRecTrpc as trpc`, and the only two `solarRecTrpc.solarRecDashboard.*` references swap to bare `trpc.solarRecDashboard.*` since the alias unifies both. `AddressChecker.tsx` (895 LOC) moved similarly — pure file move + import swap, pre-blocked on this PR because its only server calls are `abpSettlement.cleanMailingData` and `verifyAddresses`. EarlyPayment.tsx compat shim: its 2 `trpc.abpSettlement.startContractScanJob` + `trpc.abpSettlement.getJobStatus` call sites swapped to `solarRecTrpc.*` (the page itself moves with full Task 5.10 later). App.tsx legacy `/abp-invoice-settlement` + `/address-checker` URLs replaced with Wouter `<Redirect />` to `/solar-rec/abp-invoice-settlement` + `/solar-rec/address-checker`; SolarRecApp.tsx adds both new routes wrapped in `<PermissionGate moduleKey="abp-invoice-settlement">` and `<PermissionGate moduleKey="address-checker">`. Module keys already registered in `shared/solarRecModules.ts`. No schema migration. **Task 2.3 (cross-month contamination override fix) deferred** — bundled with Task 5.9 in the original plan but excluded from PR-A scope to keep this migration focused on mechanics. tsc clean; 544/544 vitest pass. |
| 2026-04-27 | Task 5.8 PR-B — Migrate DIN Scrape Manager to standalone | Done | [#136](https://github.com/rmgopaul/coherence-app/pull/136) | New `server/_core/solarRecDinScrapeRouter.ts` houses the 10 `dinScrape.*` procs that previously lived in main `dinScrapeRouter` (`startJob`, `stopJob`, `resumeJob`, `deleteJob`, `getJobStatus`, `listJobs`, `getResults`, `getDins`, `debugRaw`, `exportDinsCsv`). All gated on `requirePermission("din-scrape-manager", level)` — 6 read / 3 edit / 1 admin (`deleteJob`). Cross-tenant safety: ownership checks switched from `job.userId !== ctx.user.id` to `job.scopeId !== ctx.scopeId` per the architectural-split rule "data visibility is team-wide within a scope". CSG portal credential lookup in `startJob` keeps `ctx.userId` (the `integrations` table is per-user; multi-user team-shared CSG access is future work). `solarRecAppRouter` composition + `SOLAR_REC_ROUTER_ROOTS` gain `dinScrape`. Client: `DinScrapeManager.tsx` (931 LOC) moved from `client/src/features/dashboard/` to `client/src/solar-rec/pages/` with the standard aliased `solarRecTrpc as trpc` import. App.tsx legacy `/din-scrape-manager` URL replaced with Wouter `<Redirect />` to `/solar-rec/din-scrape-manager`; SolarRecApp.tsx adds the new route wrapped in `<PermissionGate moduleKey="din-scrape-manager">`. **`server/routers/jobRunners.ts` deleted entirely** — `dinScrapeRouter` was its last export after the Task 5.9 PR-A cleanup. Module key already registered in `shared/solarRecModules.ts` (Task 5.1 #56 baseline). No schema migration (PR-A handled scopeId in #119). tsc clean; 544/544 vitest pass. **Phase 5 router-migration arc effectively complete** for the `_core/` standalone — the only remaining wrong-side feature on the main router is Task 5.10 (Early Payment + Invoice Match Dashboard, which has the EarlyPayment.tsx compat shim from #133). |
| 2026-04-27 | Task 5.10 — Migrate Early Payment + Invoice Match Dashboard to standalone | Done | [#137](https://github.com/rmgopaul/coherence-app/pull/137) | Pure file moves + permission gates + Wouter redirects — no server router changes. By the time this task ran, both pages had already lost every server call to the main router: `EarlyPayment.tsx` already routed `solarRecDashboard.*` (via Task 5.5 PR #110) and `abpSettlement.{startContractScanJob, getJobStatus}` (via the Task 5.9 PR-A compat shim in #133) through `solarRecTrpc`; `InvoiceMatchDashboard.tsx` is a pure client-side page (CSV parse + match logic + persisted state, zero trpc calls). The dual-import shim in `EarlyPayment.tsx` (separate `trpc` and `solarRecTrpc`) collapsed into the standard `solarRecTrpc as trpc` alias used by every other migrated page. Co-located `client/src/features/dashboard/invoiceMatch/` helper directory (`helpers.ts`, `index.ts`, `types.ts`) moved alongside the page to `client/src/solar-rec/pages/invoiceMatch/`. Module keys: `early-payment` and `invoice-match` (both already registered in `shared/solarRecModules.ts`). App.tsx legacy `/early-payment` and `/invoice-match-dashboard` URLs replaced with Wouter `<Redirect />` to `/solar-rec/early-payment` and `/solar-rec/invoice-match-dashboard`; SolarRecApp.tsx adds both new routes wrapped in `<PermissionGate>`. **The "wrong-side features today" list in CLAUDE.md is now empty** — every feature in the original Phase 5 wrong-side migration plan has shipped. The list is preserved as historical context. tsc clean; 544/544 vitest pass. |
| 2026-04-27 | Task 2.3 — ABP Settlement cross-month contamination override fix | Done | [#139](https://github.com/rmgopaul/coherence-app/pull/139) | The original symptom: open ABP Invoice Settlement at month A, set an override on row R, change the month input to month B, set a different override on row R, switch back to month A — the original month-A override was gone. Root cause: `manualOverridesByRowId` was initialized once at mount from `localStorage["abp-overrides:" + buildMonthKey()]`, but the persistence `useEffect` was keyed by the live `monthKey` state, so changing months (a) wrote month A's still-in-memory overrides to month B's localStorage key (corrupting B), then (b) never reloaded month B's stored data into state. Two-keystroke contamination on every transition. Fix is two-part. (1) New helper `isCompleteMonthKey(value: string): boolean` in `client/src/lib/abpSettlement/utils/dateUtils.ts` — strict `^\d{4}-(0[1-9]|1[0-2])$` regex over `value.trim()`. (2) The persistence useEffect now bails if the resolved `monthKey` isn't a complete YYYY-MM, suppressing per-keystroke writes during typing (`2026-0`, `2026-`, etc.) that would otherwise spew orphan localStorage keys AND make the writes-before-reads contamination near-instant. (3) New `handleMonthKeyChange` callback wraps `setMonthKey`: when the new value is a complete YYYY-MM AND resolves to a different month than the current, it FLUSHES current overrides under the *previous* month's storage key and LOADS the destination month's stored overrides into in-memory state before the swap. Mid-typing values just call `setMonthKey` (no flush, no load). The page's month-load path on `applyLoadedRun` is unaffected because it sets `manualOverridesByRowId` from the saved-run payload, not from localStorage. New unit test file `client/src/lib/abpSettlement/utils/dateUtils.test.ts` with 7 cases covering `buildMonthKey` (3) + `isCompleteMonthKey` (4 — accepts 01–12, trims whitespace, rejects mid-typing values, rejects out-of-range months 00/13+, rejects malformed strings). Vitest config gains `client/src/lib/**/*.test.ts` to the include glob so these tests run in CI. tsc clean; 551/551 vitest pass (up from 544). Manual DoD: month-A override → switch to month B → set different value → switch back to A → month-A value preserved. |
| 2026-04-27 | Task 5.13 PR-1 — Migrate DeliveryTrackerTab off raw rows | Done | [#142](https://github.com/rmgopaul/coherence-app/pull/142) | First of 6 dashboard-tab migrations off raw `datasets[k].rows`. New `server/services/solar/buildDeliveryTrackerData.ts` exports the pure `buildDeliveryTrackerData({scheduleRows, transferRows})` aggregator (structural copy of the client version at `client/src/solar-rec-dashboard/lib/buildDeliveryTrackerData.ts` — helpers `clean`, `parseNumber`, `parseDate`, `buildDeliveryYearLabel`, `toPercentValue`, `UTILITY_PATTERNS` inlined rather than reaching into client/lib because moving them to `shared/` would touch ~50 files; divergence detector is the matched test files on each side). Plus `getOrBuildDeliveryTrackerData(scopeId)` wrapper that hashes the active batch IDs of `deliveryScheduleBase` + `transferHistory` to a 16-char SHA-256, fast-paths through `solarRecComputedArtifacts` (`artifactType: "deliveryTracker"`) for cache hits, recomputes synchronously on miss via `loadDatasetRows` for both srDs* tables, writes back. **superjson** for cache serde (not plain JSON) so the `Date` fields in `yearStart`/`yearEnd` round-trip through the cache the same way they round-trip through tRPC. New tRPC query `solarRecDashboard.getDashboardDeliveryTrackerAggregates` (read-gated on `solar-rec-dashboard`) returns `{...deliveryTrackerData, _runnerVersion: "data-flow-pr5_13_deliverytracker@1"}`. Client: `SolarRecDashboard.tsx`'s `useMemo(() => buildDeliveryTrackerData({...datasets...}))` is gone, replaced with a `useQuery` gated on `isDeliveryTrackerTabActive` with `staleTime: 60_000`; the `EMPTY_DELIVERY_TRACKER_DATA` import remains for the loading-state fallback. New server-side test `server/services/solar/buildDeliveryTrackerData.test.ts` with 11 cases (mirrors the client test in lockstep — divergence detector). tsc clean; 563/563 vitest pass (up from 551). DeliveryTrackerTab is the first of the 6 tabs that the data-flow architecture (PR-8) flagged for migration; CLAUDE.md updated to mark it done and reference the canonical template for the remaining 5. |
| 2026-04-27 | Task 5.13 PR-2 — Migrate AlertsTab + TrendsTab delivery-pace off raw rows | Done | [#144](https://github.com/rmgopaul/coherence-app/pull/144) | Second tab migration. The `buildTrendDeliveryPace(deliveryScheduleBase.rows, transferDeliveryLookup)` useMemo was duplicated across `AlertsTab` (the only raw-row read in that tab) and `TrendsTab` (one of three). Both call sites now hit the new `solarRecDashboard.getDashboardTrendDeliveryPace` tRPC query. New `server/services/solar/buildTrendDeliveryPace.ts` exports the pure aggregator (structural copy of `client/src/solar-rec-dashboard/lib/helpers/trends.ts:buildTrendDeliveryPace`) plus a `getOrBuildTrendDeliveryPace(scopeId, now)` wrapper that hashes (`scheduleBatchId`, `transferBatchId`, `UTC day`) into the cache key — daily granularity is enough since `now` only participates in active-window detection and the time-elapsed expected-pace calculation. Cache: synchronous fast-path through `solarRecComputedArtifacts` (`artifactType: "trendDeliveryPace"`); plain JSON cache serde works because the result has no Date fields. Server reads `srDsDeliverySchedule` rows on cache miss + reuses the existing `buildTransferDeliveryLookupForScope` (already cached separately). `_runnerVersion: "data-flow-pr5_13_trenddeliverypace@1"` carried on every response. Client: `AlertsTab` drops the `deliveryScheduleBase` + `transferDeliveryLookup` props (tab now reads `systems`, `datasets[k].uploadedAt`, and the new query — *zero* core-dataset row arrays); gains an `isActive: boolean` prop to gate the query. `TrendsTab` swaps its `trendDeliveryPace` useMemo for the same query (PARTIAL: `convertedReads`-driven `trendProductionMoM` + `trendTopSiteIds` useMemos remain because `convertedReads` is not yet row-backed — Task 5.12 will unblock the rest). Parent `SolarRecDashboard.tsx` drops 2 props from each tab's call site. New server-side test `buildTrendDeliveryPace.test.ts` with 11 cases (no client-side test exists for `trends.ts`; this server file is now the SOT). tsc clean; 574/574 vitest pass (up from 563). **AlertsTab is the 2nd of 6 tabs fully off raw rows.** |
| 2026-04-27 | Task 5.13 PR-3 — Migrate ContractsTab + AnnualReviewTab off raw rows | Done | [#146](https://github.com/rmgopaul/coherence-app/pull/146) | Third tab migration — first to do TWO tabs in one PR via a shared aggregator. ContractsTab and AnnualReviewTab were both running near-identical `useMemo`s over `deliveryScheduleBase.rows` to bucket per-(contract, deliveryStartDate). The shared aggregator at `server/services/solar/buildContractVintageAggregates.ts` returns the union of fields both tabs need (`pricedProjectCount` for ContractsTab, `reportingProjectCount` + `reportingProjectPercent` for AnnualReviewTab). Per-tab sort + downstream roll-ups (`contractSummaryRows`, `annualVintageRows`, `annualContractSummaryRows`, etc.) stay client-side because they don't read raw rows. The cached server entrypoint `getOrBuildContractVintageAggregates(scopeId)` is the most ambitious aggregator yet — it has to replicate the parent's `part2EligibleSystemsForSizeReporting` derivation server-side: load `srDsAbpReport` rows, filter via the inlined `isPart2VerifiedAbpRow`, build the 3-way ID-match (portalSystemId ∨ applicationId ∨ trackingId) against the system snapshot, and produce `eligibleTrackingIds` + `recPriceByTrackingId` + `isReportingByTrackingId` from the snapshot subset. Then load `srDsDeliverySchedule` rows + the cached `transferDeliveryLookup`, and run the pure aggregator. Cache key bundles abpReport batch + deliveryScheduleBase batch + transferHistory batch + system-snapshot hash; superjson cache serde because `deliveryStartDate: Date \| null` round-trips. New tRPC query `solarRecDashboard.getDashboardContractVintageAggregates` (read-gated on `solar-rec-dashboard`). Client: ContractsTab + AnnualReviewTab drop their 4-5 derived-state props (`deliveryScheduleBase`, `eligibleTrackingIds`, `recPriceByTrackingId`, `transferDeliveryLookup`, plus `systemsByTrackingId` for AnnualReviewTab); each tab gains an `isActive: boolean` prop to gate the query. Both tabs apply their own sort locally (ContractsTab: contractId-then-date; AnnualReviewTab: date-then-contractId). Parent `SolarRecDashboard.tsx` drops 4 props from one call site and 5 from the other. New server-side test `buildContractVintageAggregates.test.ts` with 11 cases. `_runnerVersion: "data-flow-pr5_13_contractvintage@1"`. tsc clean; 585/585 vitest pass (up from 574). **ContractsTab + AnnualReviewTab are the 3rd and 4th of 6 tabs fully off raw rows.** Remaining: TrendsTab (partial — `convertedReads` blocked on Task 5.12) + ApplicationPipelineTab (blocked on `abpIccReport3Rows` migration). |
| 2026-04-27 | Task 5.13 cleanup — de-duplicate aggregator helpers + cache wrapper | Done | [#151](https://github.com/rmgopaul/coherence-app/pull/151) | Self-review-driven follow-up to PRs #142/#144/#146 surfaced ~150 LOC of duplicated helpers (clean / parseNumber / parseDate / toPercentValue / getDeliveredForYear copy-pasted across the three aggregator files), 3 hand-written copies of the cache state machine, an unsafe `as readonly SnapshotSystem[]` cast on the snapshot's `unknown[]` return, inconsistent superjson import patterns and cache-write error handling, a duplicated `lookupFor` test fixture, completely untested cache code paths, and a misleading "matched test files" comment in PR-3. Extracted: (1) `aggregatorHelpers.ts` with the shared pure helpers + canonical server-side `SnapshotSystem` shape + `extractSnapshotSystems` runtime validator; (2) `withArtifactCache.ts` with a generic `withArtifactCache(scopeId, artifactType, hash, serde, recompute, rowCount)` wrapper exposing `jsonSerde<T>()` + `superjsonSerde<T>()`; (3) `aggregatorTestFixtures.ts` with the shared `buildTransferDeliveryLookupFixture`. PR-3's unsafe cast replaced with the runtime-validating `extractSnapshotSystems` (substitutes safe defaults for missing/wrong-typed fields). PR-3 gains an early bail when `abpReportBatchId` is null (no Part-2 verification → empty result, skips wasted snapshot+lookup loads). 33 new tests (28 helpers + 5 cache-wrapper covering hit/miss/corrupt/write-fail/Date round-trip via mocked DB). Aggregator file LOC dropped from 1,419 to 1,176 (−243). No behavior change — same wire payloads, same cache keys, same outputs (verified by 33 pre-existing aggregator output tests). tsc clean; 618/618 vitest pass (up from 585). |
| 2026-04-27 | Task 5.13 PR-4 — Migrate TrendsTab production charts off raw rows | Done | [#153](https://github.com/rmgopaul/coherence-app/pull/153) | Closes the TrendsTab migration. The two remaining raw-row useMemos (`trendProductionMoM` over `convertedReads.rows` for the month-over-month top-10-sites chart + `trendTopSiteIds` derived from that chart's first row) move server-side as a single `getDashboardTrendsProduction` query. Unblocked by Task 5.12 PR-10 (#152) which made `convertedReads` row-backed. New `server/services/solar/buildTrendsProduction.ts` exports the pure aggregator (mirrors the client useMemo body byte-for-byte: same MAX-per-(site,month) bucketing, same Wh→kWh delta, same top-10 sort, same chart pivot) plus a cached entrypoint via `withArtifactCache` with `jsonSerde` (no Date fields). Cache key bundles only the `convertedReads` batch ID — the result has no time-of-day component (unlike PR-2's daily `now` bucket). Output shape `{chartRows, topSiteIds}` returned in one payload so the tab gets both former useMemo results from a single query. New tRPC query `getDashboardTrendsProduction` read-gated on `solar-rec-dashboard`. Client: TrendsTab drops the `convertedReads` prop entirely (was the last raw-row prop in this tab); replaces both useMemos + the inline `useMemo` import with the new query. Parent `SolarRecDashboard.tsx` drops 1 prop from the TrendsTabLazy call site. Pre-existing client edge case faithfully preserved + documented in source: `if (rawWh > existing)` with `existing` defaulting to 0 silently drops first readings of value 0 — captured in the dedicated test "preserves the pre-existing client behavior where first reading == 0 is dropped". Real prod data is always > 0 (cumulative meters) so the case rarely matters; cleanup queued as a follow-up. New server-side test `buildTrendsProduction.test.ts` with 10 cases (chart deltas, MAX-per-month, non-positive-delta drop, name fallback, invalid-row skips, top-10 cap, chronological sort). `_runnerVersion: "data-flow-pr5_13_trendsproduction@1"`. tsc clean; 628/628 vitest pass (up from 618). **TrendsTab is now fully off raw rows — 5 of 6 tabs done.** Remaining: ApplicationPipelineTab (now unblocked — `abpIccReport3Rows` is row-backed via Task 5.12 PR-9). |
| 2026-04-27 | Task 5.13 PR-5 — Migrate ApplicationPipelineTab off raw rows (TASK 5.13 COMPLETE) | Done | [#156](https://github.com/rmgopaul/coherence-app/pull/156) | Closes Task 5.13. Two server-side aggregators replace the tab's two raw-row `useMemo`s. (1) `server/services/solar/buildAppPipelineMonthly.ts` — Part 1 / Part 2 / Interconnected monthly buckets joining `srDsAbpReport` (Part 1 + Part 2 detection, deduped via `resolvePart2ProjectIdentity`) + `srDsGeneratorDetails` (interconnected by GATS Unit ID + Date Online with mid-month parse). System snapshot supplies `installedKwAc` for the AC-kW fallback when generator rows lack a kW column; pulled off the validated snapshot via `extractSnapshotSystems` plus a typed-cast for the `installedKwAc` field that the canonical `SnapshotSystem` subset doesn't include. (2) `server/services/solar/buildAppPipelineCashFlow.ts` — vendor-fee / CC-auth / additional-collateral monthly buckets joining `srDsAbpReport` Part-2 rows (filtered server-side via `isPart2VerifiedAbpRow`) + `srDsAbpCsgSystemMapping` + `srDsAbpIccReport3Rows` + cached contract scan results from `getLatestScanResultsByCsgIds`. The Financials-tab `localOverrides` Map (per-csgId vendor-fee / additional-collateral % overrides) is sent as query input — the cache key includes a stable hash of the override map (sorted-key serialize before SHA-256), so cache misses fire only when the user actually changes an override. Cache hits are common because most users never enter overrides. Both queries use `withArtifactCache` + `jsonSerde` (no Date fields in either output). New tRPC queries `solarRecDashboard.getDashboardAppPipelineMonthly` + `getDashboardAppPipelineCashFlow` (the second takes an `overrides` input). Client: `AppPipelineTab` drops 7 props (4 raw datasets + `systems` + `part2VerifiedAbpRows` + `contractScanResults`); keeps `localOverrides` (sent as query input) + `financialCsgIdCount` (data-availability indicator); gains `isActive: boolean` to gate both queries. Both raw-row `useMemo`s replaced with `useQuery`s; downstream filters (`pipelineRows3Year/12Month`, `cashFlowRows3Year/12Month`) unchanged because they consume the aggregate, not raw rows. Parent `SolarRecDashboard.tsx` drops 7 props from the `AppPipelineTabLazy` call site. Also extends `aggregatorHelpers.ts` with 5 new helpers shared with the new aggregators: `roundMoney`, `parseDateOnlineAsMidMonth`, `parseAbpAcSizeKw`, `parseGeneratorDetailsAcSizeKw`, `resolvePart2ProjectIdentity`, plus the `GENERATOR_DETAILS_AC_SIZE_HEADERS` constant. New tests: `buildAppPipelineMonthly.test.ts` (9 cases) + `buildAppPipelineCashFlow.test.ts` (10 cases) — fixtures exercise dedupe, future-date filter, AC-kW fallback, prior-year fields, GCV-from-qty-times-price fallback, override application, scan-override fallback, multi-project month aggregation. `_runnerVersion`s: `data-flow-pr5_13_apppipelinemonthly@1` + `data-flow-pr5_13_apppipelinecashflow@1`. tsc clean; 647/647 vitest pass (up from 628). **Task 5.13 is now complete — all 6 dashboard tabs (DeliveryTrackerTab, AlertsTab, ContractsTab, AnnualReviewTab, TrendsTab, ApplicationPipelineTab) are fully off raw `datasets[k].rows` consumption.** |
| 2026-04-27 | Task 6.1 — Pre-computed supplement correlations | Done | [#158](https://github.com/rmgopaul/coherence-app/pull/158) | First Phase 6 task. Replaces the dashboard's adherence-only Supplements card with real effect-size signals computed nightly, eliminating the per-render correlation work the on-demand `supplements.runCorrelation` proc was doing. New table `supplementCorrelations(id, userId, supplementId, metric, windowDays, lagDays, computedAt, cohensD, pearsonR, onN, offN, onMean, offMean, insufficientData)` with unique index `(userId, supplementId, metric, windowDays, lagDays)` so the nightly job idempotently overwrites in place. Migration `0049_add_supplement_correlations.sql` (drizzle-kit collision pattern: renamed from auto-generated 0045 to 0049, snapshot moved to match, journal `idx 45` retagged to `0049_*`). **Migration applied to prod via `drizzle-kit migrate` BEFORE merging code PR** (per CLAUDE.md schema migration safety rule); table existence + columns verified via direct mysql2 query. New module `server/db/supplementCorrelations.ts` with `upsertSupplementCorrelation` (idempotent INSERT … ON DUPLICATE KEY UPDATE) + `getTopSignalsForUser(userId, limit)` ordered by `ABS(cohensD) DESC, windowDays DESC` filtering `insufficientData = false`. New `server/services/supplements/correlationNightly.ts` with `runNightlySupplementCorrelationsForUser(userId, now)` that: (1) lists active+locked supplement defs, (2) loads max-window metrics + supplement-log range once per user (8N round-trip avoidance), (3) iterates `4 metrics × 2 windows × N supplements`, calling `analyzeCorrelation` per slice, (4) upserts each result. Lag fixed at 0 for nightly compute; lagged variants stay accessible via the existing on-demand `supplements.runCorrelation`. Wired into `captureDailySnapshotForUser` after the daily-metrics upsert; wrapped in try/catch so a correlation failure can't break the rest of the snapshot. New tRPC procedure `supplements.getTopSignals({limit?: 1..20})` joins each row with the supplement's name and returns `{supplementId, supplementName, metric, windowDays, lagDays, cohensD, pearsonR, onN, offN, onMean, offMean, computedAt}[]`. Client: `SupplementsFeedCell.tsx` adds a `topSignals` query (30-min refetch interval to match nightly cadence) and renders top-signal rows (supplement name + metric label + Cohen's d) in place of the recent-logs list when signals exist. Falls back to the previous adherence display (recent logs) when no signals are available — fresh install, brand-new supplement, or insufficient-data ladders. Header `LOGGED X/Y` count preserved as the primary stat. New server-side test `correlationNightly.test.ts` with 7 cases covering: empty defs short-circuit, ignores non-active+locked supplements, writes 8 slices per eligible supplement, uses correct (supp, metric, window) tuples, flags insufficient data, computes real means with sufficient data, filters logs by definitionId per supplement (mocked DB helpers via `vi.hoisted` + `vi.mock` matching the established pattern). DoD: after one nightly run, `getTopSignalsForUser` returns rows with non-null `cohensD` and `insufficientData=false` for any supplement with ≥7 days each in on/off groups. tsc clean; 654/654 vitest pass (up from 647). |
| 2026-04-27 | Task 5.14 — Retire `getDatasetAssembled` + collapse Step 1 / DataQuality / cold-load reads to row-table primitives | Done | [#163](https://github.com/rmgopaul/coherence-app/pull/163), [#165](https://github.com/rmgopaul/coherence-app/pull/165), [#169](https://github.com/rmgopaul/coherence-app/pull/169), [#172](https://github.com/rmgopaul/coherence-app/pull/172), [#175](https://github.com/rmgopaul/coherence-app/pull/175) | 6 sub-PRs over a single day retiring the legacy chunked-CSV reassembly path. **PR-1** persisted scalar `rowCount` on `CsvDataset` + `SerializedCsvDataset` so the IndexedDB record carries the row count without the row array. **PR-2** swapped the `dataHealthSummary` useMemo's `datasets[k].rows.length` + `datasets[k].uploadedAt` reads for `datasetSummariesByKey[k].rowCount` + `lastUpdated` (already in the parent's `getDatasetSummariesAll` query). **PR-3** swapped the Step 1 upload UI's "X rows loaded" badge from `dataset.rows.length` to `dataset.rowCount`. Side-quest: removed the stale 3 MB auto-sync size guard (`MAX_REMOTE_DATASET_SYNC_ESTIMATED_CHARS` + `estimateDatasetRemotePayloadChars` + the warning notice) since the chunking layer at `REMOTE_DATASET_CHUNK_CHAR_LIMIT = 250 KB` had already eliminated the underlying constraint — surfaced as a false-positive "Schedule B too large for auto sync (3.0 MB)" notice the user observed. **PR-4** moved DataQualityTab's `dataQualityUnmatched` cross-reference reconciliation server-side: new aggregator `server/services/solar/buildDataQualityReconciliation.ts` (pure set-difference math + cached entrypoint via `withArtifactCache` keyed on the deliveryScheduleBase + convertedReads batch IDs); new tRPC query `getDashboardDataQualityReconciliation` returning `{inScheduleNotMonitoring, inMonitoringNotSchedule, matchedPercent}` capped at 10,000 IDs per side; client tab gated the query on `isActive` so it only fires when DataQualityTab is the focused tab. Replaced the original 2-paginated-reads plan with the dedicated aggregator since the result is small (a few thousand IDs at worst) and the alternative would have meant 76 sequential 500-row round-trips through `getDatasetRowsPage`. 9 unit tests on the aggregator (empty inputs, perfect match, schedule-only / monitoring-only mismatches, lowercase normalization, system_id fallback, repeat-row dedup, partial overlap). **PR-5** removed the `getRemoteDatasetAssembled` mutation declaration + ref from `SolarRecDashboard.tsx` and deleted the assembled-batch try/catch in `loadSingleDataset`, letting cold hydration always take the per-key route (`getRemoteDataset` per source, chunked at `REMOTE_DATASET_CHUNK_CHAR_LIMIT = 250 KB`). Scope tightened from the original plan: dropping `.rows` outright would have regressed 14 serializer/save/audit paths in the dashboard parent + the Schedule B Apply merge that reads `prev.deliveryScheduleBase?.rows` — the auto-sync save path migration is deferred to a follow-up workstream. The 50–150 MB single-response memory bombs that crashed Chrome tabs in the 2026-04-26 OOM events are eliminated by this PR alone; PR-6 just removes the now-orphan code. **PR-6** deleted the `getDatasetAssembled` tRPC procedure (~167 LOC) + its private helpers (`assembleFromFetchedOrFallback`, `getSourceManifestSize`, `MAX_ASSEMBLED_SOURCE_PAYLOAD_BYTES`, `MAX_ASSEMBLED_SOURCE_CHUNK_KEYS`, `SourceManifestEntry` type) + the orphan `getSolarRecDashboardPayloadsBatch` DB helper (only caller was `getDatasetAssembled`). Net delta: **352 LOC removed** (377 deletions − 25 insertions, the inserts being pointer comments). Scope deviation: the client-side `parseChunkPointerPayload` helper in `SolarRecDashboard.tsx` was kept — the plan said to delete it but a fresh grep showed two active callers (`loadPayloadByKey` at line 5111 and the snapshot-logs reassembly at line 5409) parsing the per-key `getDataset` response, exactly like the per-page copies in EarlyPayment + DeepUpdateSynthesizer that the plan correctly preserved. Helpers kept: `dashboardLoadSemaphore`, `isHeapOverSoftLimit`, `loadDashboardPayloadSingleFlight` (used by `getDataset` + `getOrCreateRemoteDataset`); `solarRecDashboardStorage` table + `_rawSourcesV1` manifest format (still authored by `saveDataset`, still read by `getDataset`). After PR-6: tsc clean; 705/705 vitest pass; `git grep getDatasetAssembled` returns only docstring/comment references. Gate after 5.14 partially met — server-side procedure removal + cold-load OOM elimination shipped; full collapse of the parent's in-memory `datasets[k].rows` state is a follow-up workstream because each consumer (auto-sync, signature builder, audit log, Schedule B Apply, sources merge, IndexedDB serializer) wants a different replacement. CLAUDE.md data-flow section rewritten to drop the deprecation grandfather clause. |
| 2026-04-28 | Task 8.1 — Job runner contract extraction | Done | TBD | First Phase 8 task. Pulls the shared "iterate pending items with bounded concurrency, increment success/failure counters, poll for cancellation" pattern out of `contractScanJobRunner`, `dinScrapeJobRunner`, `scheduleBImportJobRunner`, and `csgScheduleBImportJobRunner` (each had it copy-pasted) into `server/services/core/jobRunner.ts`. New helper `runJobWithAtomicCounters<TItem>({jobId, pendingItems, concurrency, isCancelled, processItem, incrementCounter, logError?})` returns `{cancelled, processed, successes, failures}`. The helper owns: the `mapWithConcurrency` wrap, per-item cancellation check before invoking `processItem`, per-item try/catch (an exception counts as a failure), atomic counter increment after each item, aggregate result. Caller still owns: loading the job + verifying it's not terminal, marking running, provider-specific setup (CSG portal login, OAuth refresh strategy), per-item work in `processItem`, terminal-status update. Counter-write failures are logged via the optional `logError` callback (default `console.warn`); local accounting still tracks the outcome the worker reported. Migrated all four runners: each runner's per-item helper now returns `{outcome: "success" \| "failure"}` instead of doing the counter increment inline; the `mapWithConcurrency` call site is replaced with `runJobWithAtomicCounters({...})`. Cancellation polling stays in each runner (cadence varies — contract scan polls every 3 items, din scrape rate-limits to once every 5s, csg schedule B polls every item) and gets handed to the helper as the `isCancelled` callback. Schedule B runner's two `mapWithConcurrency` passes (initial + retry-pass) both migrated. Net delta: ~280 LOC removed across the 4 runners, ~190 LOC added in `jobRunner.ts` + tests, +135 LOC of explanatory comments. Added explicit `RUNNER_VERSION` constants to the 3 previously-unversioned runners (`CONTRACT_SCAN_RUNNER_VERSION = "contract-scan-runner@1"`, `SCHEDULE_B_IMPORT_RUNNER_VERSION = "schedule-b-import-runner@1"`, `CSG_SCHEDULE_B_IMPORT_RUNNER_VERSION = "csg-schedule-b-import-runner@1"`); bumped `DIN_SCRAPE_RUNNER_VERSION` from `@14` to `@15`. New test `jobRunner.test.ts` with 8 cases covering empty input, every-item-success path, processItem-thrown-error → failure routing, soft-fail outcome, mid-stream cancellation short-circuit, counter-write failure logged but accounting tracks outcome, mid-work cancellation via `helpers.isCancelled`, concurrency-limit observance. tsc clean; 662/662 vitest pass (up from 654). Sets up the contract Phase 8 Task 8.2 will consume: a uniform job-runner shape lets the `/solar-rec/jobs` index page poll all four runners with one handler. |
| 2026-04-28 | Task 8.2 — `/solar-rec/jobs` index page | Done | TBD | Second Phase 8 task. Builds the unified `/solar-rec/jobs` surface that polls all four runners through a single handler — exactly the consumer Task 8.1's contract extraction was set up for. New db helper module `server/db/jobsIndex.ts` exposes `listRecentJobsAcrossRunners(scopeId, limit)` which fans out one `SELECT … LIMIT N` per job table (`contractScanJobs`, `scheduleBImportJobs`, `dinScrapeJobs`) in `Promise.all`, normalizes each to a shared `JobsIndexRow` shape (`{id, runnerKind, status, total, successCount, failureCount, currentItem, error, startedAt, stoppedAt, completedAt, createdAt, updatedAt}`), and merges by `compareJobsIndexRows` (createdAt DESC, updatedAt as tiebreaker). The CSG Schedule B runner shares a table with the regular Schedule B import so both surface as one `schedule-b-import` runnerKind — the dashboard's Delivery Tracker tab already distinguishes upload-sourced from CSG-sourced internally. Limit clamped to [1, 100] per table → max 300 rows total, well under CLAUDE.md's 1 MB-per-procedure hard rule. Helper exports `isLiveJobStatus(status)` for the tri-state {queued, running, stopping} check used by both server (decoration) and client (poll cadence) so the live-job semantics stay in one place. New standalone router `server/_core/solarRecJobsRouter.ts` with one query `getJobsIndex({limitPerRunner?: 1..100}) → {_runnerVersion, jobs[]}` gated on `requirePermission("jobs", "read")`. The procedure decorates each row with `liveOnThisProcess: boolean` derived from the in-process `isContractScanRunnerActive` / `isDinScrapeRunnerActive` / `isScheduleBImportRunnerActive` / `isCsgScheduleBImportRunnerActive` registries — useful for diagnosing "DB says running but no runner is actually crunching" on multi-instance deploys. Runner version `solar-rec-jobs@1` ships per the CLAUDE.md observability rule. Mounted on `solarRecAppRouter` as `jobs: solarRecJobsRouter`. New page `client/src/solar-rec/pages/JobsIndex.tsx` (≈260 LOC): one shadcn Table with columns Runner / Status / Progress / Current / Started / Updated. Status badge variants map {running, stopping}→default, queued→secondary, completed→outline, failed→destructive. Progress shows `processed / total (pct%)` with separate failed-count line. Live rows render a green pulse dot when `liveOnThisProcess` is true. Row click navigates to the runner's manager: contract-scan → `/solar-rec/contract-scrape-manager`, din-scrape → `/solar-rec/din-scrape-manager`, schedule-b-import → `/solar-rec/dashboard?tab=delivery-tracker` (the dashboard already honors `?tab=` for tab selection). Polling cadence is dynamic via React Query v5's `refetchInterval: (query) => …` callback — 3000ms while any row is live, 30000ms otherwise. `refetchOnWindowFocus: true` keeps the table fresh after the user returns from another tab. `_runnerVersion` rendered in the page footer for at-a-glance deploy verification. Wired the route in `SolarRecApp.tsx` lazy-loaded behind `<PermissionGate moduleKey="jobs">`. Added a "Jobs" entry under the Main sidebar group in `SolarRecSidebar.tsx` (using `ListChecks` icon) gated by `canSee("jobs")` — sits below the two Monitoring entries so the daily flow reads top-to-bottom (Dashboard → Monitoring → Jobs → Tools → Meter Reads). Module key `jobs` already existed in `shared/solarRecModules.ts` (added to the registry pre-emptively in Task 5.1). New test `server/db/jobsIndex.test.ts` with 6 cases covering: live-status set, non-live status returns false, non-live for unknown statuses (defensive), createdAt-desc sort, updatedAt tiebreaker, null-timestamp-as-epoch behavior, multi-runner interleaving by createdAt rather than runner kind. tsc clean; 669/669 vitest pass (up from 662). DoD: a user with `jobs: read` lands on `/solar-rec/jobs` and sees live + recent runs across all four runners in one table; clicking a row hands off to the correct manager; while any job is live the page polls every 3 seconds. |
| 2026-04-28 | Task 9.1 — Solar Applications as the system registry | Done | TBD | First Phase 9 task. Lays the foundation for the Portfolio Workbench by giving every Phase 9 surface a server-side `getSystemByCsgId` primitive — the canonical "look up one system by CSG ID" lookup the system detail page (Task 9.4) and worksets (Task 9.2) will both consume. New db helper module `server/db/systemRegistry.ts` exports `getSystemByCsgId(scopeId, csgId)` and `resolveSystemRegistryBatchIds(scopeId)`. The lookup walks the canonical join chain `csgId → srDsAbpCsgSystemMapping → systemId → srDsSolarApplications + srDsContractedDate`, falling back to `srDsSolarApplications.applicationId === csgId` (or `.systemId === csgId`) for legacy single-tenant data that pre-dates the mapping dataset. Active batch IDs for all three datasets resolve in one Promise.all on `solarRecActiveDatasetVersions`; if Solar Applications isn't active the helper returns `null` immediately (no work is meaningful without the registry). The applications query uses `or(inArray(applicationId, [systemId, csgId]), inArray(systemId, [systemId, csgId]))` so a single round-trip covers both the mapping-resolved and legacy paths. Returns a typed `SystemRegistryRecord` with the fields Phase 9 needs (csgId, abpId, applicationId, systemId, trackingSystemRefId, stateCertificationNumber, systemName, installedKwAc/Dc, recPrice, totalContractAmount, annualRecs, contractType, installerName, county, state, zipCode, contractedDate) — primitives only, no raw CSV passthrough. New standalone sub-router `server/_core/solarRecSystemsRouter.ts` exposes two queries: `getByCsgId({csgId}) → {_runnerVersion, system}` for the single-system path used by the detail page, and `getManyByCsgId({csgIds: 1..500}) → {_runnerVersion, systems[]}` for workset detail panes. Both gated on `requirePermission("portfolio-workbench", "read")` — the canonical Phase 9 module key from Task 5.1. The batch variant dedupes input internally (so a list with repeats only pays the lookup cost once) and returns the result as a parallel array aligned 1:1 with the input order; missing systems surface as `null` slots distinct from "row found but field is null". Runner version `solar-rec-systems@1`. Mounted on `solarRecAppRouter` as `systems: solarRecSystemsRouter`. New test `server/db/systemRegistry.test.ts` with 10 cases covering: empty active versions returns nulls, active rows map to slots, ignores non-target dataset keys, empty csgId short-circuits before DB, missing solarApplications dataset returns null, full join chain with all three datasets active, legacy applicationId fallback when mapping is empty, null contractedDate when dataset isn't active, full miss returns null, csgId trimmed before lookup. Mocks `_core` getDb + withDbRetry via the established `vi.hoisted` pattern from `solarRecPermissions.test.ts` so the join-chain logic is exercised without spinning up MySQL. tsc clean; 688/688 vitest pass (up from 678). DoD per the plan: given any CSG ID from the current 32,664-row Solar Applications dataset, the procedure returns the correct joined record. Unblocks Task 9.2 (worksets — needs `getManyByCsgId` to surface workset contents) and Task 9.4 (detail page — needs `getByCsgId` for the header section). |
| 2026-04-28 | Task 9.2 — Saved CSG-ID worksets (Phase 9 MVP) | Done | TBD | Second Phase 9 task. Lays the workset primitive that Task 9.3 will replace every paste-IDs textarea with and that Task 9.4's detail page will use as the breadcrumb back to a multi-system context. New table `idWorksets(id, scopeId, createdByUserId, lastEditedByUserId, name, description, csgIdsJson, csgIdCount, createdAt, updatedAt)` with unique index `(scopeId, name)` for picker dedupe + `(scopeId, updatedAt)` for the most-recently-edited list ordering. CSG IDs stored as JSON-stringified `string[]` in `mediumtext` (~16 MB headroom; proc layer caps at 10 K IDs/workset). Migration `0050_add_id_worksets.sql` (drizzle-kit collision pattern: auto-generated 0046 collided with `0046_add_srds_annual_production_estimates.sql`, renamed to 0050, snapshot moved, journal `idx 46 → 50` retagged, restored origin/main 0046_snapshot.json). **Migration applied to prod via `drizzle-kit migrate` BEFORE merging code PR** (per CLAUDE.md schema migration safety rule); table existence + columns verified via direct mysql2 `DESCRIBE idWorksets` query. New module `server/db/idWorksets.ts` (~280 LOC) exports 6 helpers: `createIdWorkset`, `getIdWorkset`, `listIdWorksets` (returns summaries — no csgIds — so the picker can show 100+ entries cheaply), `updateIdWorkset` (patch-style — only provided fields update; `lastEditedByUserId` always set on success), `deleteIdWorkset` (returns boolean for present/absent), `appendCsgIdsToWorkset` (order-preserving merge; existing IDs keep position, new IDs append in input order, dupes dropped at both ends). Plus `normalizeCsgIds` (pure, exposed for testability — trims, drops empties, dedupes first-occurrence). Two typed errors `IdWorksetNameConflictError` + `IdWorksetNotFoundError` so the proc layer can map cleanly to `CONFLICT` / `NOT_FOUND` tRPC codes. Insert duplicate-key detection via `ER_DUP_ENTRY` code AND errno 1062 for compatibility. Validation runs BEFORE `getDb()` so misuse surfaces a clean error regardless of connection state (critical for tests). New standalone sub-router `server/_core/solarRecWorksetsRouter.ts` with 6 procedures matching the helpers: `list`/`get` gated on `requirePermission("portfolio-workbench", "read")`, `create`/`update`/`delete`/`append` gated on `edit`. The `editedByUserId` is always pulled from `ctx.userId` (no client-supplied parameter — eliminates impersonation surface). All 6 procs ship `_runnerVersion: "solar-rec-worksets@1"`. Input validation via Zod: csgIds `string[]` 1..10 K, names ≤255 chars, descriptions ≤2 K chars, ids ≤64 chars. Mounted on `solarRecAppRouter` as `worksets: solarRecWorksetsRouter`. New test `server/db/idWorksets.test.ts` with 17 cases covering: `normalizeCsgIds` purity (trim+dedupe+order-preserving, hostile non-string input, empty array); `createIdWorkset` happy path with normalized IDs read back through detail, name-required validation, duplicate-key → `IdWorksetNameConflictError` mapping; `getIdWorkset` miss → null + JSON parse on hit + malformed-JSON → empty csgIds (defensive); `listIdWorksets` summary shape (no csgIds) + empty scope; `updateIdWorkset` patch behavior with merged read-back + `IdWorksetNotFoundError` on missing row; `deleteIdWorkset` true-on-present + false-on-absent (no delete attempted); `appendCsgIdsToWorkset` order-preserving merge with cross-set dedupe + not-found mapping. Mocks `_core` via the established `vi.hoisted` pattern; the stub builder thenable supports both selects and inserts/updates/deletes by always resolving to canned rows in registration order. tsc clean; 705/705 vitest pass (up from 688). DoD per the plan: create a workset of 10 CSG IDs; retrieve; append; delete. Second user in same scope sees the same worksets — guaranteed by the `scopeId`-keyed list query and team-visible read gate. Unblocks Task 9.3 (workset selectors on every job page) and provides the multi-system entry point for Task 9.4 (system detail). |
