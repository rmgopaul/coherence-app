<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.health.READ_STEPS" />
  <uses-permission android:name="android.permission.health.READ_DISTANCE" />
  <uses-permission android:name="android.permission.health.READ_FLOORS_CLIMBED" />
  <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
  <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
  <uses-permission android:name="android.permission.health.READ_EXERCISE" />
  <uses-permission android:name="android.permission.health.READ_SLEEP" />
  <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
  <uses-permission android:name="android.permission.health.READ_VO2_MAX" />
  <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE" />
  <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
  <uses-permission android:name="android.permission.health.READ_RESPIRATORY_RATE" />
  <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
  <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE" />
  <uses-permission android:name="android.permission.health.READ_BLOOD_GLUCOSE" />
  <uses-permission android:name="android.permission.health.READ_WEIGHT" />
  <uses-permission android:name="android.permission.health.READ_HEIGHT" />
  <uses-permission android:name="android.permission.health.READ_BODY_FAT" />
  <uses-permission android:name="android.permission.health.READ_BODY_WATER_MASS" />
  <uses-permission android:name="android.permission.health.READ_BASAL_METABOLIC_RATE" />
  <uses-permission android:name="android.permission.health.READ_NUTRITION" />
  <uses-permission android:name="android.permission.health.READ_HYDRATION" />
  <uses-permission android:name="android.permission.health.READ_BODY_TEMPERATURE" />
  <uses-permission android:name="android.permission.health.READ_SKIN_TEMPERATURE" />
  <uses-permission android:name="android.permission.health.READ_POWER" />
  <uses-permission android:name="android.permission.health.READ_SPEED" />

  <queries>
    <package android:name="com.google.android.apps.healthdata" />
  </queries>

  <application
    android:name=".CoherenceApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/Theme.Material3.DayNight.NoActionBar">

    <activity
      android:name=".MainActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
      android:name=".PermissionsRationaleActivity"
      android:exported="false" />

    <activity
      android:name=".auth.OAuthRedirectActivity"
      android:exported="true"
      android:launchMode="singleTask">
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="coherence" android:host="auth-callback" />
      </intent-filter>
    </activity>

    <activity-alias
      android:name=".ViewPermissionUsageActivity"
      android:exported="true"
      android:permission="android.permission.START_VIEW_PERMISSION_USAGE"
      android:targetActivity=".PermissionsRationaleActivity">
      <intent-filter>
        <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
        <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
      </intent-filter>
      <intent-filter>
        <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
      </intent-filter>
    </activity-alias>

    <receiver
      android:name=".sync.BootReceiver"
      android:enabled="true"
      android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>

    <!--
      WorkManager's SystemForegroundService runs our Health Connect sync
      workers as foreground services. Android 14+ requires the service's
      foregroundServiceType attribute to cover every type the worker sets
      on its ForegroundInfo. Our workers set FOREGROUND_SERVICE_TYPE_HEALTH
      (0x100); the library's default manifest declares 0x0, so the system
      rejects startForeground with IllegalArgumentException. Merge "health"
      into the library's service declaration.
    -->
    <service
      android:name="androidx.work.impl.foreground.SystemForegroundService"
      android:foregroundServiceType="health"
      tools:node="merge" />

    <!-- Home-screen dashboard widget -->
    <receiver
      android:name=".widget.CoherenceDashboardWidgetReceiver"
      android:enabled="true"
      android:exported="true"
      android:label="Coherence Dashboard">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <intent-filter>
        <action android:name="com.coherence.healthconnect.widget.ACTION_REFRESH" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/coherence_dashboard_widget_info" />
    </receiver>

    <!-- King widget — Z Fold-sized variant. Same data pipeline as
         CoherenceDashboardWidgetReceiver; just a different layout and
         provider info (see coherence_king_widget_info.xml). -->
    <receiver
      android:name=".widget.CoherenceKingWidgetReceiver"
      android:enabled="true"
      android:exported="true"
      android:label="Coherence King">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <intent-filter>
        <action android:name="com.coherence.healthconnect.widget.king.ACTION_REFRESH" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/coherence_king_widget_info" />
    </receiver>

    <!-- King • Left — half of the side-by-side King pair. Owns the
         KoD hero + tasks + inbox + next event. Pair with King · Right
         to fill the Z Fold inner display (Samsung One UI caps a
         single widget at ~half the inner display, hence the split). -->
    <receiver
      android:name=".widget.CoherenceKingLeftWidgetReceiver"
      android:enabled="true"
      android:exported="true"
      android:label="Coherence King · Left">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <intent-filter>
        <action android:name="com.coherence.healthconnect.widget.king.left.ACTION_REFRESH" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/coherence_king_left_widget_info" />
    </receiver>

    <!-- King • Right — half of the side-by-side King pair. Owns
         markets + news + sports. Pair with King · Left. -->
    <receiver
      android:name=".widget.CoherenceKingRightWidgetReceiver"
      android:enabled="true"
      android:exported="true"
      android:label="Coherence King · Right">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <intent-filter>
        <action android:name="com.coherence.healthconnect.widget.king.right.ACTION_REFRESH" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/coherence_king_right_widget_info" />
    </receiver>

    <!-- Habits widget — Loop-style tappable tiles. Each tile logs the
         habit for today and recolors to indicate completion. Shares
         the same WidgetDataWorker pipeline as the dashboard / King
         widgets. -->
    <receiver
      android:name=".widget.CoherenceHabitsWidgetReceiver"
      android:enabled="true"
      android:exported="true"
      android:label="Coherence Habits">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <intent-filter>
        <action android:name="com.coherence.healthconnect.widget.habits.ACTION_REFRESH" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/coherence_habits_widget_info" />
    </receiver>
  </application>

</manifest>
